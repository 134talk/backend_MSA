server:
  port: 0 # 랜덤하게 지정

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defalutZone: http://localhost:8761/eureka  
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    
spring:
  application:
    name: statistics-service  
    
  kafka:
    bootstrap-servers: ${KAFKA_CLUSTER01:43.201.21.16:9092},${KAFKA_CLUSTER02:13.125.37.231:9092},${KAFKA_CLUSTER03:15.164.50.105:9092}

